diff --git a/lib/Cargo.toml b/lib/Cargo.toml
index 02edb98..cf0009a 100644
--- a/lib/Cargo.toml
+++ b/lib/Cargo.toml
@@ -28,14 +28,14 @@ humantime = "2"
 once_cell = "1.17"
 regex = "1"
 rusqlite = ">=0.31, <0.33"
-sequoia-cert-store = { version = "0.7", default-features = false }
+sequoia-cert-store = { version = "0.7", default-features = false, path = "../../sequoia-cert-store" }
 sequoia-directories = "0.1"
-sequoia-keystore = { version = "0.7" }
-sequoia-ipc = { version = "0.36", default-features = false }
-sequoia-net = { version = "0.30", default-features = false }
-sequoia-openpgp = { version = "2", default-features = false, features = ["compression"] }
-sequoia-policy-config = "0.8"
-sequoia-wot = { version = "0.15", default-features = false }
+sequoia-keystore = { version = "0.7", path = "../../sequoia-keystore/keystore" }
+sequoia-ipc = { version = "0.36", default-features = false, path = "../../sequoia/ipc" }
+sequoia-net = { version = "0.30", default-features = false, path = "../../sequoia/net" }
+sequoia-openpgp = { version = "2", default-features = false, features = ["compression"], path = "../../sequoia/openpgp" }
+sequoia-policy-config = { version = "0.8", path = "../../sequoia-policy-config" }
+sequoia-wot = { version = "0.15", default-features = false, path = "../../sequoia-wot/lib" }
 tempfile = "3.1"
 terminal_size = ">=0.2.6, <0.5"
 textwrap = { version = ">=0.15, <0.17", default-features = false, features = ["smawk", "unicode-width"] }
@@ -62,11 +62,11 @@ clap = ["dep:clap"]
 
 [target.'cfg(not(windows))'.dev-dependencies]
 # Enables a crypto backend for the tests:
-sequoia-openpgp = { version = "2", default-features = false, features = ["crypto-nettle", "__implicit-crypto-backend-for-tests"]  }
+sequoia-openpgp = { version = "2", default-features = false, features = ["crypto-nettle", "__implicit-crypto-backend-for-tests"], path = "../../sequoia/openpgp"  }
 
 [target.'cfg(windows)'.dev-dependencies]
 # Enables a crypto backend for the tests:
-sequoia-openpgp = { version = "2", default-features = false, features = ["crypto-cng", "__implicit-crypto-backend-for-tests"] }
+sequoia-openpgp = { version = "2", default-features = false, features = ["crypto-cng", "__implicit-crypto-backend-for-tests"], path = "../../sequoia/openpgp" }
 
 [package.metadata.docs.rs]
 # Enables a crypto backend for the docs.rs generation:
diff --git a/tool/Cargo.toml b/tool/Cargo.toml
index 0d08487..482eceb 100644
--- a/tool/Cargo.toml
+++ b/tool/Cargo.toml
@@ -22,10 +22,10 @@ maintenance = { status = "actively-developed" }
 aho-corasick = "1"
 dirs = "5"
 fs_extra = "1"
-sequoia-openpgp = { version = "2", default-features = false }
-sequoia-autocrypt = { version = "0.26", default-features = false }
-sequoia-net = { version = "0.30", default-features = false }
-sequoia-policy-config = "0.8"
+sequoia-openpgp = { version = "2", default-features = false, path = "../../sequoia/openpgp" }
+sequoia-autocrypt = { version = "0.26", default-features = false, path = "../../sequoia/autocrypt" }
+sequoia-net = { version = "0.30", default-features = false, path = "../../sequoia/net" }
+sequoia-policy-config = { version = "0.8", path = "../../sequoia-policy-config" }
 anyhow = "1.0.18"
 chrono = "0.4.10"
 clap = { version = "4", features = ["derive", "env", "string", "wrap_help"] }
@@ -63,8 +63,8 @@ roff = "0.2.1"
 serde = { version = "1.0.137", features = ["derive"] }
 sequoia = { path = "../lib", default-features = false, features = ["clap"] }
 sequoia-man = "0.3"
-sequoia-openpgp = { version = "2", default-features = false }
-sequoia-net = { version = "0.30", default-features = false }
+sequoia-openpgp = { version = "2", default-features = false, path = "../../sequoia/openpgp" }
+sequoia-net = { version = "0.30", default-features = false, path = "../../sequoia/net" }
 textwrap = ">=0.15, <0.17"
 typenum = "1"
 cfg-if = "1"
@@ -104,11 +104,11 @@ subplot = ["culpa", "subplot-build", "subplotlib"]
 
 [target.'cfg(not(windows))'.dev-dependencies]
 # Enables a crypto backend for the tests:
-sequoia-openpgp = { version = "2", default-features = false, features = ["crypto-nettle", "__implicit-crypto-backend-for-tests"]  }
+sequoia-openpgp = { version = "2", default-features = false, features = ["crypto-nettle", "__implicit-crypto-backend-for-tests"], path = "../../sequoia/openpgp"  }
 
 [target.'cfg(windows)'.dev-dependencies]
 # Enables a crypto backend for the tests:
-sequoia-openpgp = { version = "2", default-features = false, features = ["crypto-cng", "__implicit-crypto-backend-for-tests"] }
+sequoia-openpgp = { version = "2", default-features = false, features = ["crypto-cng", "__implicit-crypto-backend-for-tests"], path = "../../sequoia/openpgp" }
 
 [package.metadata.docs.rs]
 # Enables a crypto backend for the docs.rs generation:
